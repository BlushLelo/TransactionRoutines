spring:
  datasource:
    url: jdbc:mysql://172.17.0.1:3306/database
    username: admin
    password: admin
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      use-new-id-generator-mappings: false
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: false

server:
  port: 8081
